<script type="text/javascript" src="{{ asset('js/app.min.js') }}"></script>

<script type="text/javascript">
    <!-- Bootstrap JS -->
    $('a[rel="popup"]').live('click', function(e) {
        e.preventDefault();
        $('#modal-process').find('h3').html($(this).attr("title"));
        $('#modal-link').attr("href", $(this).attr("href"));
        $('#modal-link').attr("download", $(this).attr("title"));
        $.get(this.href, 
    	    function(data) {
              	$('#modal-content').text(data).html();
            },
            "text"
            )
        .complete(function(){ 
        	prettyPrint();
            $('#modal-process').modal('show');
        });
    });
    $("a[rel=tooltip-right]").tooltip( {
        animation:true,
        placement: "right"
        });
    $("a[rel=tooltip-left]").tooltip( {
        animation:true,
        placement: "left"
        });
    $("a[rel=tooltip-top]").tooltip( {
        animation:true,
        placement: "top"
        });
</script>
